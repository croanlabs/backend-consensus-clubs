const assert = require('assert');
// const rewardService = require('../services/reward');
// const db = require('../config/database');

// const ReferralReward = db.ReferralReward;

// ReferralReward: a new row in Reward table
describe('services.reward', () => {
  it('Referral reward process inserts a row into table Reward', async () => {
    // const textReward = 'test referral reward';
    // const [idReward] = await rewardService.referralReward(textReward);
    // const reward = await Reward.findById(idReward);
    assert.equal();
  });
});
// ReferralReward: a new row in ReferralReward table
// ReferralReward: if the user has received 500 merits
// // ReferralReward: if a new notification is created
